spring.application.name=backend

# Conexión a la base de datos en Docker
spring.datasource.url=jdbc:postgresql://localhost:5432/farmaciadb
spring.datasource.username=admin_db
spring.datasource.password=password_fuerte123

# Configuración de JPA (Mapeo de objetos a tablas)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect